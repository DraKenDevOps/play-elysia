export type TIndex = {
    [key: string]: any;
};

export interface IIndex {
    [key: string]: any;
}

export type TQueryUser = {
    user_id: string;
    password: string;
    email: string;
    role: string;
    status: string;
};
